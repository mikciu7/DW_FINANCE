<<<<<<< HEAD
version: '3.8'

services:
  backend:
    build: ./backend
    container_name: backend
    env_file: .env
    expose:
      - "8000"
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: frontend
    ports:
      - "80:80" # Tylko ten port wystawiamy na świat!
    depends_on:
      - backend
    restart: unless-stopped
=======
services:

  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DB_HOST=${DB_HOST}
      - DB_NAME=${DB_NAME}
      - DB_USER=${DB_USER}
      - DB_PASS=${DB_PASS}
      - DB_PORT=${DB_PORT:-5432}
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        # Podczas lokalnego dev backend jest dostępny przez localhost
        # Na ECS zmień na URL ALB, np. https://api.twoja-domena.com/api
        VITE_API_URL: http://localhost:8000/api
    ports:
      - "80:80"
    depends_on:
      backend:
        condition: service_healthy
>>>>>>> 94b742f29c5bb7db156fd5c552b9e3d5f0e72d8d
